const short = `

1234567890123456789012345678901234567890123456789012345Writing a minimap in code can greatly enhance the user experience by providing a bird's eye view of the entire document being worked on. This feature can be especially useful when working with large documents where navigating and finding one's place can be a challenge. In this essay, we'll explore the basics of what a minimap is, why it's useful, and how to create one.

A minimap is a small representation of the entire content of a document. It's often displayed in a separate window or pane and allows users to quickly see where they are in the document and navigate to other parts of it. In some cases, a minimap can be used to show an overview of an entire website, with each page represented by a small thumbnail.

One of the primary benefits of a minimap is that it provides an aerial view of the document, allowing users to more easily understand its structure and quickly navigate to any section. This can be especially useful when working with documents that are several pages long, such as reports or large technical documents. With a minimap, users can see the entire document at a glance and quickly zoom in or out to see different parts of it.

To create a minimap in code, we can use HTML5 canvas API. First, we need to create a canvas element, which will be used to display the minimap. We can set the size of the canvas to match the size of the document being edited. Then, we use JavaScript to draw the contents of the document on the canvas. To keep the canvas in sync with the scroll position of the editor, we add an event listener for the scroll event and redraw the minimap whenever the user scrolls the document.

Overall, a minimap in code is a useful tool for enhancing the user experience when working with large documents. By providing an aerial view of the document and allowing users to quickly navigate to different parts of it, a minimap can help users be more productive and efficient in their work.


  
1234567890123456789012345678901234567890123456789012345Writing a minimap in code can greatly enhance the user experience by providing a bird's eye view of the entire document being worked on. This feature can be especially useful when working with large documents where navigating and finding one's place can be a challenge. In this essay, we'll explore the basics of what a minimap is, why it's useful, and how to create one.

A minimap is a small representation of the entire content of a document. It's often displayed in a separate window or pane and allows users to quickly see where they are in the document and navigate to other parts of it. In some cases, a minimap can be used to show an overview of an entire website, with each page represented by a small thumbnail.

One of the primary benefits of a minimap is that it provides an aerial view of the document, allowing users to more easily understand its structure and quickly navigate to any section. This can be especially useful when working with documents that are several pages long, such as reports or large technical documents. With a minimap, users can see the entire document at a glance and quickly zoom in or out to see different parts of it.

To create a minimap in code, we can use HTML5 canvas API. First, we need to create a canvas element, which will be used to display the minimap. We can set the size of the canvas to match the size of the document being edited. Then, we use JavaScript to draw the contents of the document on the canvas. To keep the canvas in sync with the scroll position of the editor, we add an event listener for the scroll event and redraw the minimap whenever the user scrolls the document.

Overall, a minimap in code is a useful tool for enhancing the user experience when working with large documents. By providing an aerial view of the document and allowing users to quickly navigate to different parts of it, a minimap can help users be more productive and efficient in their work.

1234567890123456789012345678901234567890123456789012345Writing a minimap in code can greatly enhance the user experience by providing a bird's eye view of the entire document being worked on. This feature can be especially useful when working with large documents where navigating and finding one's place can be a challenge. In this essay, we'll explore the basics of what a minimap is, why it's useful, and how to create one.

A minimap is a small representation of the entire content of a document. It's often displayed in a separate window or pane and allows users to quickly see where they are in the document and navigate to other parts of it. In some cases, a minimap can be used to show an overview of an entire website, with each page represented by a small thumbnail.

One of the primary benefits of a minimap is that it provides an aerial view of the document, allowing users to more easily understand its structure and quickly navigate to any section. This can be especially useful when working with documents that are several pages long, such as reports or large technical documents. With a minimap, users can see the entire document at a glance and quickly zoom in or out to see different parts of it.

To create a minimap in code, we can use HTML5 canvas API. First, we need to create a canvas element, which will be used to display the minimap. We can set the size of the canvas to match the size of the document being edited. Then, we use JavaScript to draw the contents of the document on the canvas. To keep the canvas in sync with the scroll position of the editor, we add an event listener for the scroll event and redraw the minimap whenever the user scrolls the document.

Overall, a minimap in code is a useful tool for enhancing the user experience when working with large documents. By providing an aerial view of the document and allowing users to quickly navigate to different parts of it, a minimap can help users be more productive and efficient in their work.

1234567890123456789012345678901234567890123456789012345Writing a minimap in code can greatly enhance the user experience by providing a bird's eye view of the entire document being worked on. This feature can be especially useful when working with large documents where navigating and finding one's place can be a challenge. In this essay, we'll explore the basics of what a minimap is, why it's useful, and how to create one.

A minimap is a small representation of the entire content of a document. It's often displayed in a separate window or pane and allows users to quickly see where they are in the document and navigate to other parts of it. In some cases, a minimap can be used to show an overview of an entire website, with each page represented by a small thumbnail.

One of the primary benefits of a minimap is that it provides an aerial view of the document, allowing users to more easily understand its structure and quickly navigate to any section. This can be especially useful when working with documents that are several pages long, such as reports or large technical documents. With a minimap, users can see the entire document at a glance and quickly zoom in or out to see different parts of it.

To create a minimap in code, we can use HTML5 canvas API. First, we need to create a canvas element, which will be used to display the minimap. We can set the size of the canvas to match the size of the document being edited. Then, we use JavaScript to draw the contents of the document on the canvas. To keep the canvas in sync with the scroll position of the editor, we add an event listener for the scroll event and redraw the minimap whenever the user scrolls the document.

Overall, a minimap in code is a useful tool for enhancing the user experience when working with large documents. By providing an aerial view of the document and allowing users to quickly navigate to different parts of it, a minimap can help users be more productive and efficient in their work.

1234567890123456789012345678901234567890123456789012345Writing a minimap in code can greatly enhance the user experience by providing a bird's eye view of the entire document being worked on. This feature can be especially useful when working with large documents where navigating and finding one's place can be a challenge. In this essay, we'll explore the basics of what a minimap is, why it's useful, and how to create one.

A minimap is a small representation of the entire content of a document. It's often displayed in a separate window or pane and allows users to quickly see where they are in the document and navigate to other parts of it. In some cases, a minimap can be used to show an overview of an entire website, with each page represented by a small thumbnail.

One of the primary benefits of a minimap is that it provides an aerial view of the document, allowing users to more easily understand its structure and quickly navigate to any section. This can be especially useful when working with documents that are several pages long, such as reports or large technical documents. With a minimap, users can see the entire document at a glance and quickly zoom in or out to see different parts of it.

To create a minimap in code, we can use HTML5 canvas API. First, we need to create a canvas element, which will be used to display the minimap. We can set the size of the canvas to match the size of the document being edited. Then, we use JavaScript to draw the contents of the document on the canvas. To keep the canvas in sync with the scroll position of the editor, we add an event listener for the scroll event and redraw the minimap whenever the user scrolls the document.

Overall, a minimap in code is a useful tool for enhancing the user experience when working with large documents. By providing an aerial view of the document and allowing users to quickly navigate to different parts of it, a minimap can help users be more productive and efficient in their work.
`;


const medium = `

function factorial(n) {
  if (n === 0 || n === 1) {
    return 1;
  } else {
    return n * factorial(n - 1);
  }
/* Ignored */  } /* Hello world */

function isNumber(string) {
  return /^\d+(\.\d*)?$/.test(string)
}

const NUM_TRIALS = 100;
const MAX_NUMBER = 100;

function factorial(n) {
  if (n === 0 || n === 1) {
    return 1;
  } else {
    return n * factorial(n - 1);
  }
/* Ignored */  } /* Hello world */

function isNumber(string) {
  return /^\d+(\.\d*)?$/.test(string)
}

const NUM_TRIALS = 100;
const MAX_NUMBER = 100;

function factorial(n) {
  if (n === 0 || n === 1) {
    return 1;
  } else {
    return n * factorial(n - 1);
  }
/* Ignored */  } /* Hello world */

function isNumber(string) {
  return /^\d+(\.\d*)?$/.test(string)
}

const NUM_TRIALS = 100;
const MAX_NUMBER = 100;

function factorial(n) {
  if (n === 0 || n === 1) {
    return 1;
  } else {
    return n * factorial(n - 1);
  }
/* Ignored */  } /* Hello world */

function isNumber(string) {
  return /^\d+(\.\d*)?$/.test(string)
}

const NUM_TRIALS = 100;
const MAX_NUMBER = 100;

function factorial(n) {
  if (n === 0 || n === 1) {
    return 1;
  } else {
    return n * factorial(n - 1);
  }
/* Ignored */  } /* Hello world */

function isNumber(string) {
  return /^\d+(\.\d*)?$/.test(string)
}

const NUM_TRIALS = 100;
const MAX_NUMBER = 100;
`;

const long = `
  function factorial(n) {
    if (n === 0 || n === 1) {
      return 1;
    } else {
      return n * factorial(n - 1);
    }
/* Ignored */  } /* Hello world */
// XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX
  const NUM_TRIALS = 100;
  const MAX_NUMBER = 100;

  function isNumber(string) {
    return /^\d+(\.\d*)?$/.test(string)
  }
  /**
   * Multi
   * Line
   * 
   * Comment
   */

  function getRandomNumber(min, max) {
    return Math.floor(Math.random() * (max - min + 1) + min);
  }

  function isNumber2(string) {
    return /^\d+(\.\d*)?$/.test(string)
  }
  const min = 1; // minimum value for random number
  const max = 100; // maximum value for random number
  for (let i = 0; i < 10; i++) { // loop 10 times
    const randomNumber = getRandomNumber(min, max); // get a random number between min and max
    console.log("Random Number " + String(i+1): + String(randomNumber)); // output the random number to the console
  }
  console.log("Done!"); // output "Done!" to the console when finished
  let sumOfFactorials = 0;
  for (let i = 0; i < NUM_TRIALS; i++) {
    const randomInt = Math.floor(Math.random() * MAX_NUMBER) + 1;
    sumOfFactorials += factorial(randomInt);
  } /* Hello world */
  console.log('The sum of factorials is: ', sumOfFactorials);
  const NUM_TRIALS = 100;
  const MAX_NUMBER = 100;

  function isNumber3(string) {
    return /^\d+(\.\d*)?$/.test(string)
  }
  /**
   * Multi
   * Line
   * 
   * 
   * 
   *    * Multi
  * Line
  * 
  * 
  *    * Multi
  * Line
  * 
  * XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX
  *    * Multi
  * Line
  * 
  * 
  *    * Multi
  * Line
  * 
  * 
  * 
  *    * Multi
  * Line
  * 
  * 
  *    * Multi
  * Line
  * 
  * 
  *    * Multi
  * Line
   * 
   * Comment
   */



  let sumOfFactorials = 0;
  for (let i = 0; i < NUM_TRIALS; i++) {
    const randomInt = Math.floor(Math.random() * MAX_NUMBER) + 1;
    sumOfFactorials += factorial(randomInt);
  } /* Hello world */

  console.log('The sum of factorials is: ', sumOfFactorials);

  const NUM_TRIALS = 100;
  const MAX_NUMBER = 100;

  let sumOfFactorials = 0;
  for (let i = 0; i < NUM_TRIALS; i++) {
    const randomInt = Math.floor(Math.random() * MAX_NUMBER) + 1;
    sumOfFactorials += factorial(randomInt);
  } /* Hello world */

  console.log('The sum of factorials is: ', sumOfFactorials);


  let sumOfFactorials = 0;
  for (let i = 0; i < NUM_TRIALS; i++) {
    const randomInt = Math.floor(Math.random() * MAX_NUMBER) + 1;
    sumOfFactorials += factorial(randomInt);
  } /* Hello world */

  console.log('The sum of factorials is: ', sumOfFactorials);

  const NUM_TRIALS = 100;
  const MAX_NUMBER = 100;

  let sumOfFactorials = 0;
  for (let i = 0; i < NUM_TRIALS; i++) {
    const randomInt = Math.floor(Math.random() * MAX_NUMBER) + 1;
    sumOfFactorials += factorial(randomInt);
  } /* Hello world */

  /**
   * Multi
   * Line
   * 
   * 
   * XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX
   *    * Multi
  * Line
  * 
  * 
  *   *    * Multi
  * Line
  * 
  * 
  *    * Multi
  * Line
  * 
  * 
  *    * Multi
  * Line
  * 
  * 
  * 
  *    * Multi
  * Line
  * 
  *    * Multi
  * Line
  * 
  * 
  *    * Multi
  * Line
  * 
  * XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX
  *    * Multi
  * Line
  * 
  * 
  * 
  *    * Multi
  * Line
  * 
  * 
  *    * Multi
  * Line
  * 
  * 
  *    * Multi
  * Line
   * 
   * Comment
   */

  let sumOfFactorials = 0;
  for (let i = 0; i < NUM_TRIALS; i++) {
    const randomInt = Math.floor(Math.random() * MAX_NUMBER) + 1;
    sumOfFactorials += factorial(randomInt);
  } /* Hello world */


  /**
   * Multi
   * Line
   * 
   * 
   * 
   *    * Multi
  * Line
  * 
  * 
  *   *    * Multi
  * Line
  * 
  * 
  *    * Multi
  * Line
  * 
  * 
  *    * Multi
  * Line
  * 
  * 
  * 
  *    * Multi
  * Line
  * 
  *    * Multi
  * Line
  * 
  * 
  *    * Multi
  * Line  * 
  * 
  *   *    * Multi
  * Line
  * 
  * XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX
  *    * Multi
  * Line
  * 
  * 
  *    * Multi
  * Line
  * 
  * 
  * 
  *    * Multi
  * Line
  * 
  *    * Multi
  * Line
  * 
  * 
  *    * Multi
  * Line
  * 
  * 
  *    * Multi
  * Line
  * 
  * 
  * 
  *    * Multi
  * Line
  * 
  * 
  *    * Multi
  * Line
  * 
  * 
  *    * Multi
  * Line
   * 
   * Comment  * 
  * 
  *   *    * Multi
  * Line
  * 
  * XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX
  *    * Multi
  * Line
  * 
  * 
  *    * Multi
  * Line
  * 
  * 
  * 
  *    * Multi
  * Line
  * 
  *    * Multi
  * Line
  * 
  * 
  *    * Multi
  * Line
  * 
  * 
  *    * Multi
  * Line
  * 
  * 
  * 
  *    * Multi
  * Line
  * 
  * 
  *    * Multi
  * Line
  * 
  * 
  *    * Multi
  * Line
   * 
   * Comment  * 
  * 
  *   *    * Multi
  * Line
  * 
  * 
  *    * Multi
  * Line
  * 
  * 
  *    * Multi
  * Line
  * 
  * 
  * 
  *    * Multi
  * Line
  * 
  *    * Multi
  * Line
  * 
  * 
  *    * Multi
  * Line
  * 
  * 
  *    * Multi
  * Line
  * 
  * 
  * 
  *    * Multi
  * Line
  * 
  * 
  *    * Multi
  * Line
  * XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX
  * 
  *    * Multi
  * Line
   * 
   * Comment
  * 
  * 
  *    * Multi
  * Line
  * 
  * 
  * 
  *    * Multi
  * Line
  * 
  * 
  *    * Multi
  * Line
  * 
  * 
  *    * Multi
  * Line
   * 
   * Comment
   */

  console.log('The sum of factorials is: ', sumOfFactorials);

  let sumOfFactorials = 0;
  for (let i = 0; i < NUM_TRIALS; i++) {
    const randomInt = Math.floor(Math.random() * MAX_NUMBER) + 1;
    sumOfFactorials += factorial(randomInt);
  } /* Hello world */

  console.log('The sum of factorials is: ', sumOfFactorials);

  const NUM_TRIALS = 100;
  const MAX_NUMBER = 100;

  let sumOfFactorials = 0;
  for (let i = 0; i < NUM_TRIALS; i++) {
    const randomInt = Math.floor(Math.random() * MAX_NUMBER) + 1;
    sumOfFactorials += factorial(randomInt);
  } /* Hello world */

  console.log('The sum of factorials is: ', sumOfFactorials);

  let sumOfFactorials = 0;
  for (let i = 0; i < NUM_TRIALS; i++) {
    const randomInt = Math.floor(Math.random() * MAX_NUMBER) + 1;
    sumOfFactorials += factorial(randomInt);
  } /* Hello world */

  console.log('The sum of factorials is: ', sumOfFactorials);

  const NUM_TRIALS = 100;
  const MAX_NUMBER = 100;

  let sumOfFactorials = 0;
  for (let i = 0; i < NUM_TRIALS; i++) {
    const randomInt = Math.floor(Math.random() * MAX_NUMBER) + 1;
    sumOfFactorials += factorial(randomInt);
  } /* Hello world */

  console.log('The sum of factorials is: ', sumOfFactorials);
`;

import { massive } from "./massive";

export default { short, medium, long, massive };
